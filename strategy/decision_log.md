# Never Blank — Decision Log

Не протокол разговоров. Только решения, которые меняют систему — что, почему,
и что из этого следует. Хронологический порядок, новые записи снизу.

---

## 2026-07-21 — Часть 1: позиционирование и роли

**Контекст:** Never Blank смешивал в одном потоке контента четыре разных вещи
(себя, Clarity Lab, VG Group, экспертность основателя). Аудитория не получала
ясного ответа на вопрос "зачем мне за этим следить и что мне предлагают".

**Решения:**
1. Never Blank перестаёт продавать всё сразу. Каждый канал/пост продаёт один
   уровень лестницы за раз: Never Blank → VGroup → Clarity Lab.
2. Центральное обещание Never Blank — непрерывное присутствие бизнеса без
   постоянного участия владельца. Не "AI пишет посты", не "автономный research
   engine", не "интеграции" — это механика, не обещание.
3. Clarity Lab становится эталонным бизнесом (Reference Business), через
   который демонстрируется работа системы — не второй продукт, который надо
   продавать параллельно.
4. Главная задача Never Blank — не публикация контента сама по себе, а
   накопление знаний о бизнесе клиента и постоянное улучшение стратегии на
   основе реальных результатов (цикл: Business Memory → Business Model →
   Hypothesis → Campaign → Signals → Learning → Updated Business Memory).
   **Статус: целевая архитектура v2.** Business Model слой (профитность услуг,
   поведение клиентов и т.п.) требует интеграции с реальными данными клиента,
   которой сегодня нет — не блокирует текущий запуск, но и не заявляется как
   уже работающая возможность в продающих текстах.
5. Первичный источник знаний — структурированная память, не Excel и не сырой чат.
6. Вместо одного "универсального маркетолога-продажника" — три отдельные роли
   с чёткими границами: маркетолог, продажник, аналитик экспериментов.
   Файлы: `config/prompts/role_marketer.yaml`, `role_salesperson.yaml`,
   `role_experiment_analyst.yaml`. Статус `draft` — операционные рамки для
   ручной/полу-ручной работы, ещё не зашиты в автоматизированный вызов пайплайна.

---

## 2026-07-21 — Часть 2: редакционная идентичность (worldview)

**Контекст:** вопрос "о чём Never Blank будет писать" оказался не тем вопросом.
Правильный вопрос — "почему человек будет возвращаться на страницу, даже если
сегодня ничего не собирается покупать".

**Решения:**
7. Герой контента — владелец бизнеса, не Never Blank. Never Blank — наблюдатель
   и рассказчик (документалист), а не эксперт по контенту и не голос "AI-продукта".
8. Never Blank — исследователь жизни малого бизнеса, а не эксперт по контенту.
   Контент — один из симптомов, которые он исследует, не тема сама по себе.
9. Люди покупают изменение, не продукт. Четыре формы изменения, которые
   Never Blank постоянно исследует: избавиться от проблемы, получить желаемый
   результат, снизить тревогу/риск, стать тем бизнесом, которым хочется быть.
10. Never Blank не заявляет наблюдений и статистики, которых у системы реально
    нет. Формулировки вида "я видел это уже сотый раз" запрещены без реальной
    опоры на research-сигналы (`config/research_sources.yaml`) — это то же
    правило, что и запрет маркетологу придумывать факты о клиентах (см. решение 6).
11. Тема AI/automation не запрещена навсегда, а придерживается до момента, когда
    доверие уже установлено контентом — не скрывается бесконечно и не
    используется как способ ввести читателя в заблуждение.
12. Основной документ идентичности — `strategy/worldview.md` (не "editorial
    charter", не контент-план). Из него следуют все остальные документы —
    стратегия, промпты, рубрики, темы, CTA — а не наоборот.

**Открытый вопрос (не решено сегодня):** нужно ли и как обновлять
`docs/GROWTH_STRATEGY.md` и существующие промпты (`config/prompts/`,
`config/brand_voice.md`) под это решение — следующий шаг, не часть этой записи.

---

## 2026-07-21 — Часть 3: граница территории и продажи без рекламы

**Контекст:** стратегия из Части 2 хороша на долгосрок, но клиенты нужны уже
сейчас. Также обнаружилось пересечение: "исследователь жизни малого бизнеса"
(решение 8) слишком широко — это территория VG Group, который как раз
занимается перестройкой бизнес-процессов малого бизнеса целиком (HR, операционка,
финансы). Never Blank должен быть уже.

**Решения:**
13. **Уточняет и сужает решение 8.** Never Blank не исследует малый бизнес
    целиком — это территория VG Group. Территория Never Blank: закономерности
    видимости, присутствия и запоминаемости бизнеса в голове клиента. Не лезет
    в HR, финансы, операционку, CRM. Одна тема: почему одни компании постоянно
    присутствуют в голове клиента, а другие исчезают.
14. Каждый пост должен помогать владельцу узнать себя в проблеме — не
    рассказывать о возможностях или архитектуре Never Blank.
15. Продажа происходит не через отдельные рекламные посты (не "каждый второй
    пост продаёт"), а через диагностическое продолжение наблюдения — CTA как
    логическое завершение мысли, а не рекламная вставка. Пример: "Если ты уже
    три месяца не можешь регулярно рассказывать о своём бизнесе — покажи мне,
    как сейчас устроено твоё присутствие. Я скажу, где именно система
    перестаёт работать." Инфраструктурно это уже частично существует: поля
    `sales_angle` и `soft_cta` в `config/content_matrix.yaml` генерируются для
    каждого топика — задача не строить новый механизм, а сделать формулировки
    диагностически прямыми вместо расплывчато-мягких.

**Рабочее предпочтение (не зафиксировано окончательно):** тэглайн "Never Blank
studies what keeps businesses remembered" предпочтительнее "...stays visible" —
ближе к языку потери клиента, уже используемому в `config/brand_voice.md`.

**Открытые вопросы (не решено сегодня):**
- `config/prompts/observation_discovery.yaml` сегодня не ограничен темой
  видимости/присутствия — ищет любые бизнес-наблюдения. Чтобы граница
  территории (решение 13) реально соблюдалась в генерируемом контенте, этот
  промпт рано или поздно нужно будет сузить под ту же тему.
- Узкая территория облегчает попадание в топ поиска (Google/LinkedIn), но
  не гарантирует его — для этого отдельно нужна SEO-дисциплина (ключевые
  слова, регулярность, структура блога на Wix). Не рассмотрено сегодня.

---

## 2026-07-21 — Часть 4: структура поста, Echo, разделение Editorial/Distribution

**Контекст:** пример статьи выявил, что текущий шаблон (`config/prompts/linkedin_post.yaml`
и сиблинги) слишком быстро переходит от hook к развитию мысли, не создавая
момента "это же про меня", разрешает нарратив исследования ("мы изучили 100
компаний"), и оставляет CTA на усмотрение модели в момент генерации — что на
практике означает, что модель почти всегда будет выбирать путь без продажи.

*Ниже — финальная уточнённая формулировка (заменяет черновую версию решений
16–21, которая была записана раньше в этой же сессии и оказалась слишком
жёсткой по Echo и Presence Debt).*

**Решения:**

16. **Recognition before Explanation.** До того как статья начнёт объяснять
    механизм, читатель должен узнать собственную ситуацию. Recognition — не
    случайная фраза и не CTA в конце, а отдельная функция текста, обязательный
    шаг между Observation и Explanation.
    Проверочный вопрос: может ли целевой владелец бизнеса до середины статьи
    подумать "это происходит именно со мной"? Если нет — Recognition не сработал.

17. **Research stays backstage.** Never Blank почти никогда не рассказывает о
    самом процессе исследования — не пишет "я изучил", "я проанализировал",
    "мы исследовали компании", "я решил разобраться". Читатель получает
    наблюдение, доказательства и механизм, а не отчёт о том, как система
    добывала материал. Исключение: статья прямо посвящена самому исследованию,
    методологии или неожиданному пути к открытию.

18. **CTA управляется кампанией, не моделью.** Natural Invitation не должен
    появляться или исчезать по случайному решению модели в момент генерации —
    решение принимается на уровне content matrix/кампании через явное поле,
    например `cta_mode`: `none` / `reflection` / `diagnostic` /
    `example_request` / `direct_conversation`. Если матрица требует
    диагностический CTA, модель не может заменить его нейтральной концовкой.
    Формулировки внутри выбранного режима при этом варьируются естественно.

19. **Echo отделён от CTA, но не является одинаковой концовкой каждого поста.**
    Echo — последняя мысль, которая продолжает звучать после того, как
    читатель закрыл статью. Не пересказ, не мораль, не лозунг, не "а что
    думаете?", не обязательная реклама, не универсальная фраза-заглушка.
    Механика: для каждого содержательного поста система генерирует несколько
    кандидатов Echo и включает в финальный текст сильнейший — **только когда
    он естественно заработан логикой статьи**, не принудительно. В первой
    кампании сильный Echo должен присутствовать как минимум примерно в
    каждом втором посте (LinkedIn — часто; короткий Threads-пост или прямой
    sales-пост — иногда без Echo, и это нормально).
    CTA и Echo регулируются независимо друг от друга — пост может содержать:
    Echo без CTA · CTA перед Echo · приглашение, которое само выполняет
    функцию Echo · прямой CTA без философской концовки (в отдельных
    продажных публикациях).

20. **Архитектура и последовательность запуска.** Разделение на два слоя:
    - **Editorial Strategy** — что говорит Never Blank, какую проблему
      исследует, Recognition/Explanation/Reframe/Echo, голос, CTA-логика.
      Делаем сейчас.
    - **Distribution Engine** (позже) — LinkedIn-specific distribution,
      поиск/discoverability, SEO/GEO, форматирование под платформу, Campaign
      Engine, аналитика распространения. Отдельный документ
      (`SOCIAL_DISTRIBUTION_ENGINE.md` или похожий).
    Между слоями — обязательный этап 1.5, первая живая кампания:
    ```
    1. Implement Editorial Strategy
    2. Generate and review the first campaign
    3. Publish the campaign
    4. Collect real qualitative and quantitative signals
    5. Build Distribution Engine using actual evidence
    ```
    Distribution Engine не блокирует запуск первой кампании.

21. **Named Concepts вводятся постепенно, как гипотеза, а не навечно
    утверждённый термин.** Never Blank не запускает сразу четыре новых
    термина. Первый термин для тестирования кампании: **Presence Debt**.
    Рабочее определение: *Presence Debt is the accumulated cost of the
    periods when a business disappears from customers' view and has to
    rebuild recognition, familiarity, and trust again.*
    Критерии проверки после кампании: повторяют ли люди формулировку в
    комментариях; узнают ли в ней свою ситуацию; задают ли вопросы; помогает
    ли она перейти к диагностике; не требует ли слишком длинного объяснения.
    Если термин не цепляется — не обязаны защищать его только потому, что
    он записан в стратегии. Compound Presence, Visibility System, Recognition
    Loop остаются в резерве, вводятся позже и только при естественном контексте.

22. **Аналитика включает человеческую оценку, автоматика — только сырые
    сигналы.** Автоматически собираются доступные платформенные сигналы:
    impressions, reactions, comments, reposts, clicks, profile views (если
    доступны), website visits, replies, leads, conversations. Оценки вроде
    "Recognition weak", "Echo worked", "CTA felt forced", "readers repeated
    the concept", "comments showed self-recognition" — **не автоматические
    метрики платформы**, а качественное суждение роли Аналитика экспериментов
    на основе текста публикации, комментариев, сообщений, поведения аудитории
    и сравнения с целью поста.

23. **Хештеги и SEO-friendly добавляются сейчас, но не становятся центром
    стратегии.** Можно включать в текущую имплементацию, не дожидаясь полного
    Distribution Engine, с ограничениями: хештеги остаются в publishing layer,
    не в Editorial Engine; статья сначала должна быть хорошей без хештегов;
    теги подбираются под конкретный пост/отрасль/аудиторию, не общий
    одинаковый набор под каждым постом; никакого `#success #motivation
    #business`; Named Concept можно использовать как фирменный тег только
    после того, как он несколько раз объяснён в контенте. Для LinkedIn —
    примерно 3–5 релевантных хештегов.
    SEO/GEO-friendly сейчас означает (без отдельного движка): прямо называть
    проблему; использовать ясные термины; объяснять причинно-следственную
    связь; определять собственные концепты; сохранять конкретные сущности и
    источники; писать самодостаточные предложения, понятные вне контекста;
    не прятать тему за красивыми, но бессодержательными метафорами.

**Открытый вопрос (не решено сегодня):** после первой кампании отдельно
решить, какие LinkedIn-механики действительно заслуживают полноценного
Distribution Engine — чтобы не тормозить запуск, но и не игнорировать
discoverability совсем.

---

## 2026-07-22 — Часть 5: философия статьи в Editorial Engine V2

**Контекст:** архитектура пайплайна к этому моменту выглядит так:

```
Сигнал
      ↓
Pattern / Visibility Mechanism
      ↓
Decision Lens
      ↓
Narrative Spine
      ↓
Hook
      ↓
Reader Context
      ↓
Discovery
      ↓
Story Assembly
      ↓
Never Blank Voice
      ↓
Platform Composer
```

Последовательность модулей вторична. Важнее философия статьи — что именно
меняется в самой логике текста относительно старого подхода (где героем был
упомянутый бренд — "Nike сделал X, вот что это значит для малого бизнеса").

**Решения:**

24. **Статья больше не про компанию.** Главный герой — владелец бизнеса, не
    бренд из инфоповода. Корпоративный пример — доказательство механизма,
    не сюжет. Критерий качества: статья должна работать, даже если убрать
    название компании (Nike, Getty и т.п.) полностью — если без него текст
    разваливается, значит компания была героем, а не доказательством.

25. **Статья начинается с узнавания, не с новости.** Не "Nike инвестировал
    в...", а "Многие владельцы бизнеса уверены, что проблема в отсутствии
    новых клиентов" или "Иногда бизнес перестаёт расти не потому, что стал
    хуже." Читатель должен узнать себя уже в первых абзацах — это
    продолжает и уточняет Recognition-правило из решения 16: здесь Recognition
    сдвигается ещё раньше, в сам hook, а не только в отдельный блок после Observation.

26. **После узнавания — напряжение через вопрос, не через ответ или совет.**
    Например: "Почему два похожих бизнеса получают совершенно разный поток
    клиентов?" или "Почему хорошие компании постепенно исчезают из поля
    зрения?" Читатель должен захотеть узнать механизм, а не получить его
    сразу.

27. **Расследование — самая сильная часть новой архитектуры.** Не
    объяснение с самого начала, а последовательное отбрасывание очевидных
    версий: "Можно подумать, что дело в рекламе → нет. Тогда кажется, что
    дело в цене → тоже нет → что остаётся? → появляется настоящий
    механизм." Это отдельный этап между Reader Context и Story Assembly
    (модуль Discovery в схеме выше), не то же самое, что Explanation в
    решении 16 — Discovery это путь к механизму, Explanation это сам
    механизм после того, как он найден.

28. **Компания появляется только как доказательство, встроенное в середину
    расследования, не в заголовок и не в начало.** Не "Nike сделал...", а
    "Этот же механизм хорошо виден на примере Nike...". Прямое продолжение
    решения 24 и решения 17 (research/investigation остаётся частью текста,
    но подчинён механизму, не наоборот).

29. **Reframe ломает очевидное объяснение, не просто советует больше
    делать.** Не "нужно публиковать больше", а "проблема редко в количестве
    публикаций — обычно проблема в том, что присутствие бизнеса зависит
    исключительно от свободного времени владельца." Это тот же Reframe-шаг,
    что уже есть в структуре поста (после Recognition/Explanation), здесь
    просто зафиксирован критерий: реформулировка обязана оспаривать самое
    очевидное прочтение ситуации, а не быть косметической.

30. **Бизнес-перевод отвечает на прямой вопрос "почему владельцу вообще
    должно быть не всё равно".** Конкретные, коммерческие, не эмоциональные
    последствия: потеря доверия, выпадение из памяти клиента, проседание
    входящего потока, снижение вероятности рекомендаций, рост стоимости
    привлечения. Уточняет секцию "Business and sales meaning" из брифа для
    Claude Code (`strategy/claude_code_prompt_amendment_2026-07-21.md` и
    исходный промпт) конкретным списком допустимых формулировок.

31. **CTA зависит от режима кампании, формулировка — от режима, не общий
    шаблон.** Прямое применение `cta_mode` из решения 18: `diagnostic` →
    "Если интересно понять, где именно присутствие вашего бизнеса начинает
    зависеть только от вашего времени — это уже можно разобрать отдельно."
    `reflection` → "Возможно, именно здесь стоит посмотреть на собственную
    систему иначе." Ничего нового по механике, фиксирует конкретные примеры
    формулировок по режимам.

**Открытая проблема, зафиксированная как следующий крупный шаг (не решено
сегодня, не заблокировано):** между Сигналом и Editorial Engine не хватает
отдельного слоя — **Pattern Extractor**. Сейчас пайплайн идёт как `Signal →
Editorial Engine`, и Editorial Engine вынужден сам догадываться, как
превратить корпоративную новость в историю про владельца бизнеса — это
делает качество менее стабильным (компания иногда "просвечивает" как
главный сюжет, вопреки решению 24). Предлагаемая архитектура: `Signal →
Pattern Extractor → Editorial Engine`, где Pattern Extractor встаёт перед
Decision Lens и отвечает на: какой механизм обнаружен; какая типичная
ситуация владельца соответствует этому механизму; когда этот сигнал вообще
не подходит Never Blank; какое бизнес-последствие у механизма; почему
корпоративный пример является лишь доказательством. Это следующее крупное
улучшение архитектуры, не часть текущей задачи.

---

## 2026-07-22 — Часть 6: Never Blank как аналитика рынка, ГТМ на реальных данных, приоритет MVP над архитектурой

**Контекст:** возник вопрос "мы вообще зарабатываем деньги, а если да — то как". Обсуждение прошло два круга. Первый — предложение сделать Strategy Engine (research → pattern extraction → месячная стратегия → контент) основным продуктом. Второй — GPT переформулировал это же предложение как "каноническое определение Never Blank, имеющее приоритет над всеми последующими архитектурными решениями". Второе прямо противоречит уже принятому решению 4 (Business Memory-цикл помечен как v2, не блокирует запуск) и собственному более раннему совету того же GPT (сначала продать один артефакт вручную, автоматизировать только после подтверждённого спроса).

**Решения:**

32. **Философская часть принимается сразу, без новой архитектуры под неё.**
    Контент — доказательство качества рыночного мышления Never Blank, не
    самостоятельный продукт. Compound Presence — механизм реализации
    стратегии (устойчивое накопление доверия и узнаваемости), не самоцель.
    Это не новое решение, а явное подтверждение того, что уже было записано
    в решениях 24–28 и в `strategy/worldview.md` — годится сразу, без
    доработки кода.

33. **Pipeline "Market Intelligence → Research → Pattern Extraction →
    Strategy Selection → Compound Presence Plan → Content & Distribution →
    Business Feedback → Learning" получает тот же статус, что и решение 4:
    целевая архитектура v2, долгосрочное видение, НЕ канонична и НЕ имеет
    приоритета над текущими решениями.** Она не блокирует и не заменяет
    MVP-тест ниже. Причина: приоритет момента — не более умная архитектура,
    а первая продажа. Это прямо продолжает логику решения 4, теперь
    применённую и к Strategy Engine, и к более амбициозной версии той же
    идеи.

34. **Целевая аудитория для первой кампании и холодных продаж — малые
    B2B-сервисные бизнесы с одним ЛПР: агентства (диджитал/веб/дев/креатив),
    консультанты, MSP.** Не "малый бизнес" в целом (это по-прежнему слишком
    широко для холодного захода и продолжает сужение из решения 13).
    Причина выбора — не личное знание ниши, а рыночные данные: этот сегмент
    уже живёт в LinkedIn (единственный доступный канал), уже имеет бюджетную
    строку на маркетинг, и его боль ("замолкаем, когда загружены клиентской
    работой") задокументирована независимо от Never Blank. Отдельно выделен
    сильный подсегмент: MSP-соло-предприниматели и B2B-фаундеры, которым
    нужно показать техническую компетентность, оставаясь понятными
    нетехническому покупателю.

35. **Первый платный артефакт — Monthly Growth Strategy + 4 недели
    реализации, не подписка.** Ориентир цены по рынку — $1,000–2,000 за
    первый месяц (рыночные ретейнеры на контент для малого бизнеса —
    $1,000–5,000/мес; LinkedIn-гострайтинг для фаундеров — sweet spot
    $1,000–3,500/мес). Подписка на постоянное сопровождение продаётся
    только после того, как клиент увидел ценность на пилоте.

36. **Канал первой продажи — персонализированный холодный контакт в
    LinkedIn, не рассылка и не органический пост.** LinkedIn ощутимо
    обгоняет email по отклику (5–15% при точном таргетинге против 3–8% у
    email). Сообщение не должно быть питчем — оно должно быть бесплатным
    мини-наблюдением о видимом присутствии конкретного адресата (Never
    Blank находит паттерн — ровно то, что он умеет — и показывает это в
    одном письме), которое естественно ведёт к предложению Monthly Growth
    Strategy. Это тот же принцип "продавать через демонстрацию мышления"
    (решения 24–28), применённый к одному сообщению, а не к статье.
    Дополнительно: онлайн-митап для малого бизнеса (ожидается примерно
    через неделю от 2026-07-22) — более сильная первая точка проверки, чем
    холодный LinkedIn/email, потому что даёт живую реакцию сразу.

**Открытый вопрос (не решено сегодня):** нужно ли и когда переходить от
MVP (ручная Monthly Growth Strategy + холодный контакт) к автоматизации
Strategy Engine — критерий перехода: минимум 1–2 подтверждённых платящих
клиента на пилоте, не раньше.

---

## 2026-07-22 — Часть 7: Strategy Engine строится в коде сейчас (переопределяет открытый вопрос Части 6)

**Контекст:** вместо ручной Excel-кампании (см. `strategy/never_blank_campaign_1.xlsx`,
собранной как разовый пример) Валя явно решила: месячный цикл research →
стратегия → контент-план → статьи → публикация → аналитика должен быть
реализован в коде и работать автоматически, не как ручной шаблон. Это
осознанно закрывает открытый вопрос из Части 6 раньше, чем предполагал
критерий перехода (1–2 платящих клиента) — риск принят сознательно, не
проглядели.

**Решения:**

37. **Strategy Engine строится сейчас, не после подтверждённых платящих
    клиентов.** Переопределяет открытый вопрос Части 6. `never_blank_campaign_1.xlsx`
    остаётся как эталонный пример качества (тон, структура, распределение
    cta_mode), не как процесс, который нужно повторять вручную каждый месяц.

38. **Echo — приоритет, значительно выше прежнего ориентира "примерно
    половина постов" (решение 19), но не жёсткое требование в 100% статей.**
    Система по-прежнему генерирует несколько кандидатов и не имеет права
    вставлять слабый/шаблонный Echo, если ни один кандидат не заработан
    логикой статьи — но частота "не заработано" должна быть редким
    исключением, не нормой.

39. **Публикация полностью автономна — без ручного review перед выходом в
    LinkedIn.** Единственный gate — существующий автоматический Quality
    Gate (`src/quality/gate.py`: voice/factuality/duplication checks), не
    человек. Это осознанный риск (см. открытый вопрос ниже).

40. **Текущая стратегия хранится в двух формах одновременно: в коде
    (структура, реально управляющая генерацией) и в человекочитаемом
    документе,** который можно открыть и прочитать без чтения кода — что
    выбрано на этот месяц и почему. Обновляется каждый цикл вместе с кодом,
    не отдельно от него.

41. **Отдельный от `decision_log.md` артефакт — история стратегий по
    циклам.** `decision_log.md` фиксирует архитектурные/системные решения;
    новый файл фиксирует фактические месячные стратегии и их результат:
    период → какая стратегия была выбрана и на основе какой аналитики →
    что показала практика (сработало/частично/нет, лиды, реакции, клиенты
    да/нет) → что выбрано следующим и почему. Хронологический, добавляется
    в конце каждого цикла, коммитится в git вместе с остальным.

**Открытый вопрос (риск, принятый сознательно, не решённый):** без
ручного review первые несколько статей публикуются от имени бренда без
чьей-либо проверки. Если Quality Gate пропустит что-то некачественное —
это увидит реальная аудитория раньше, чем Валя. Не блокирует запуск по
её решению, но стоит зафиксировать явно, а не молчать об этом.

---

## 2026-07-22 — Часть 8: два промпта для Strategy Engine сведены в один

**Контекст:** GPT независимо написал собственный, более детальный промпт
для той же задачи (`strategy/claude_code_prompt_strategy_engine_gpt_2026-07-22.md`),
частично противоречащий уже написанному (`strategy/claude_code_prompt_strategy_engine_2026-07-22.md`).
Расхождения разобраны и явно разрешены. Итоговый документ для отправки
Клоду Коду — `strategy/claude_code_prompt_strategy_engine_FINAL_2026-07-22.md`,
он использует GPT-версию как основу (детальнее и производственнее: weekly/
monthly review, CONTINUE/ADJUST_EXECUTION/REVIEW_STRATEGY, JSON-схемы,
fail-closed валидация, разбивка на Phase 1–4) и переопределяет четыре пункта.

**Решения:**

42. **Pattern Extractor строится как полноценный рабочий модуль сразу, не
    минимальная заглушка.** GPT-версия допускала урезанную MVP-версию "если
    полноценная реализация сейчас слишком рискованна" — отклонено. Прямое
    продолжение решения из Части 5 (Pattern Extractor был открытым
    архитектурным пунктом, откладывать его реализацию второй раз не нужно).

43. **Echo — большинство статей, не строгие 100%.** Формулировка GPT
    ("Echo обязателен. Если Echo отсутствует, материал не должен считаться
    готовым") отклонена как более жёсткая, чем нужно — подтверждает и
    уточняет решение 38: доля статей без Echo должна быть редким
    исключением, но планка качества кандидатов не понижается ради 100%
    охвата.

44. **Структура статьи: запускаем в продакшн новую 11-шаговую арку,
    существующую 9-шаговую не удаляем.** В продакшн сейчас идёт: Hook →
    Recognition → Tension → Market Observation → Investigation → Mechanism →
    Business Consequence → Reframe → Compound Presence Connection → Echo →
    Soft CTA (из GPT-промпта). Уже реализованная арка (Hook → Observation →
    Recognition → Explanation → Reframe → Echo → CTA, `config/prompts/linkedin_post.yaml`,
    решения 16–19) остаётся в коде без изменений и явно помечается в
    документации как требующая собственного тестирования/сравнения позже —
    не как ошибочная или устаревшая, просто не та, что генерирует контент
    прямо сейчас.

45. **Две технические поправки к GPT-промпту, не связанные с расхождением
    между версиями:** поле `cta_intent` из JSON-примеров GPT заменяется на
    уже существующий в коде `cta_mode`/`CTAMode` (`models.py`,
    `content_matrix.yaml`, `generator.py`, `pipeline.py`) — не создавать
    параллельное поле; и `"target_audience": "small business owners"` в
    примерах заменяется на сегмент из решения 34 (малые B2B-сервисные
    бизнесы — агентства/консультанты/MSP).

---

## 2026-07-22 — Часть 9: root cause статьи про Stellantis — стратегия была закоммичена, но ни разу не вызвана в продакшене

**Контекст:** статья про Stellantis (ценовая стратегия Topolino), опубликованная
уже после "финальной версии" промпта, нарушала практически все решения
Editorial Engine V2 (территория, герой, голос, Echo, хештеги — см. разбор
в чате от 2026-07-22). Причина оказалась не в качестве промптов, а в том,
что публикующий workflow физически не вызывал Editorial Engine.

**Что произошло:** `smoke_test_full_cycle.yml` запускал
`smoke_test_publish_analytics.py`, который на шаге 1 читал уже готовый файл
с диска (`json.loads(path.read_text())`) вместо вызова `generate_article()`.
Публикуемый файл — `reports/content_packages/3a9bd0297a166bd4_generated.json` —
был создан 8 июля 2026, за две недели до активации новой стратегии
(22 июля). Workflow не знал ни о стратегии, ни об Editorial Engine — он
проверял только публикационную инфраструктуру (Wix API, LinkedIn API,
History, analytics), но назывался "Full Publish + Analytics Cycle", что
звучало как полный цикл, включающий генерацию. Описание в самом скрипте
это честно говорило ("Uses the already-generated content package — no LLM
calls"), но читалось как техническая деталь, а не архитектурное
ограничение.

**Решения:**

46. **Smoke test и генерация+публикация — теперь два раздельных workflow с
    разными названиями и разными задачами**, а не один workflow, название
    которого вводит в заблуждение насчёт того, что он на самом деле делает.

47. **`_generated.json` теперь несёт provenance**: `strategy_id`,
    `strategy_started_at`, `generated_at` (ISO 8601) — у любого
    сгенерированного пакета контента теперь видно, какой стратегией и когда
    он был произведён.

48. **Staleness guard перед публикацией**: если `strategy_id` пакета не
    совпадает с активной стратегией, или `generated_at` раньше
    `strategy.started_at` — публикация блокируется. Устаревший пакет
    физически не может уйти в продакшен молча.

49. **`research_generate_and_publish.yml` теперь реально вызывает
    `generate_article()` с активной стратегией** — раньше генерации в CI не
    было вообще, только публикация того, что уже лежало на диске.

**Урок для протокола (не техническое решение, а процессный):** новая
стратегия была полностью закоммичена в код, прошла ревью, но ни разу не
была реально вызвана в продакшен-пути до этого dry-run — три недели работы
над Editorial Engine V2 физически не долетали до опубликованных постов, и
это не было заметно, потому что smoke test проверял правильные
инструменты (API, публикацию), но с неправильным входным материалом
(старый файл, не свежая генерация). На будущее: проверка "весь цикл
работает" должна означать буквально весь цикл, включая генерацию — тест,
который использует заранее подготовленный fixture вместо реального вызова
LLM-слоя, не может называться "full cycle" без явной оговорки в названии,
не только в комментарии в коде.

---

## 2026-07-23 — Часть 10: живой пример скорректировал два правила — тэглайн Never Blank и структура хештегов

**Контекст:** свежий сгенерированный пост по активной стратегии (пакет
`76cddc4be3a57826`) закончился строкой `𝗡𝗲𝘃𝗲𝗿 𝗕𝗹𝗮𝗻𝗸 helps founders shape
that story before silence shapes it for them.` после Echo, и шестью
хештегами вместо 3–5. Оба места были отмечены как нарушения решений 19 и
23. Увидев результат вживую, Валя скорректировала оба правила — не
согласилась с обоими замечаниями.

**Решения:**

52. **Тэглайн "Never Blank [делает X]" после Echo — разрешён.** Отменяет
    часть решения 19, запрещавшую заканчивать пост фразой вида "Never
    Blank" в виде лозунга. Формулировка решения 19 была принята без
    полной уверенности ("я была не очень у него уверена, поэтому как-то
    ушло") и не подтвердилась на живом примере — Вале нравится
    результат. Остальная часть решения 19 (Echo не пересказ, не мораль,
    не универсальная фраза-заглушка, заработан логикой статьи) остаётся
    в силе — меняется только финальный статус закрывающей строки с брендом.

53. **Хештеги: 3 брендовых + 3 отраслевых = 6 на пост, не 3–5.** Уточняет
    решение 23 (там было "примерно 3–5"). Это сознательный выбор, не
    случайный разброс: брендовые хештеги (включая хештеги по названиям
    компаний, упомянутых в статье как доказательство — Nike, Samsung и
    т.п.) нужны, чтобы пост находили по хештег-поиску, связанному именно
    с этой компанией.

**Открытый вопрос (не решено сегодня):** в примере `76cddc4be3a57826`
среди брендовых тегов был `#CompoundPresence`, хотя термин "Compound
Presence" ни разу не встречается и не объясняется в тексте самого поста —
это прямое нарушение отдельной части решения 23 ("Named Concept как
хештег — только после того, как он объяснён в контенте"), которую
решение 53 не отменяет. Нужно решить: брендовые хештеги фиксированы
всегда (`#NeverBlank` + текущий named concept кампании), или зависят от
того, реально ли термин встречается в конкретном посте.

---

## 2026-07-23 — Часть 11: root cause пустого `wix_url` — History не коммитится обратно в git

**Контекст:** пакет `76cddc4be3a57826` имел пустой `wix_url` и отсутствовал
в `strategy/published_content_index.jsonl`. Задача на диагностику отправлена
Клоду Коду (`strategy/claude_code_task_wix_url_investigation_2026-07-23.md`).

**Что произошло:** два независимых момента. (1) Wix Blog v3 API иногда не
возвращает `post.url` сразу после публикации; fallback-построение URL из
`NB_WIX_SITE_BASE_URL` + slug не срабатывает в режиме `--from-package`,
потому что в этом режиме `_resolve_post_url()` вообще не вызывается — код
просто сохраняет то, что вернул паблишер, включая пустую строку. (2)
`published_content_index.jsonl` пишется корректно во время прогона в CI
("History entry written"), но **не коммитится обратно в репозиторий** —
это уже известный архитектурный долг (`task_eff3d80f`). То, что лежит в
репозитории локально — состояние из предыдущих smoke-test прогонов
(запись про круассан-статью), не отражает реальные публикации сегодняшнего
дня.

**Решения:**

54. **`_resolve_post_url()`: всегда строить URL из `NB_WIX_SITE_BASE_URL` +
    slug, если slug известен**, не дожидаясь, пока Wix API вернёт `post.url`
    напрямую. Дополнительное требование поверх предложенного фикса:
    логировать явно, какой источник использован (URL от Wix API vs.
    сконструирован из slug) — чтобы при будущей смене структуры ссылок на
    Wix это не превратилось в тихую путаницу.

55. **`published_content_index.jsonl` коммитится в git в шаге "Mark signal
    as published"**, тем же способом, что и `published_signal_ids.txt`.
    Закрывает разрыв между решением 41 ("история коммитится в git вместе с
    остальным") и фактическим поведением — до этого фикса требование
    решения 41 не выполнялось на практике.

**Важная оговорка к Части 9 и Части 10 (пересматривает предыдущий вывод):**
поскольку до фикса 55 История не коммитилась, все выводы, сделанные сегодня
и вчера на основе чтения `published_content_index.jsonl` из локального
репозитория — включая вывод из Части 9/чата о том, что круассан-статья
была опубликована **дважды** — сделаны на устаревших данных, не на
фактическом состоянии продакшена. Этот вывод не подтверждён и подлежит
перепроверке после того, как фикс 55 будет развёрнут и История начнёт
реально отражать публикации.

---

## 2026-07-25 — Часть 12: GitHub Actions billing — два разных утечки, в разных репозиториях

**Контекст:** письмо от GitHub про переполнение бесплатного лимита Actions.
Такое уже чинили несколько недель назад — вернулось снова. Затрагивает не
только `never-blank-pipeline`, но и `clarity-lab-pipeline`, `vg-group`,
`vg-personal-pipeline` — то есть выходит за рамки Never Blank как продукта,
но фиксируется здесь, потому что решающая часть находится в этом
репозитории и здесь есть с чем сверять факт исправления. Валя явно просила
не просто записать, а потом отдельно проверить, что фикс реально сделан,
а не только обсуждён.

**Что я лично проверила в `never-blank-pipeline` (совпадает с диагнозом):**
`daily_signal_research.yml` загружает artifact с `if: always()`, включает
всю папку `reports/content_packages/` целиком (не только пакет текущего
рана) и хранит 30 дней — подтверждено построчно. `scheduled_publish.yml`
хранит отчёт 90 дней — подтверждено. Дополнительно нашла то, что не было
в исходном разборе: `live_publish_test.yml` тоже хранит 90 дней — мелкий
вклад, но той же природы, стоит поправить заодно.

**Что НЕ проверено:** сторона `clarity-lab-pipeline` (шесть Threads-ранов
в день, `Random delay (0–30 min)` в `schedule.yml` и `stories_workflow.yml`,
неслитые оптимизации в main) — у меня нет доступа к этому репозиторию,
выводы там основаны только на переданном разборе, не на независимой проверке.

**Решения (что нужно сделать — статус "не сделано, только записано"):**

56. **Never Blank — storage.** В `daily_signal_research.yml`: не грузить
    всю `reports/content_packages/`, только пакет текущего рана; не
    создавать artifact, если нужных файлов нет. `retention-days`: 30 → 3–7
    в `daily_signal_research.yml`, 90 → 3–7 в `scheduled_publish.yml` и в
    `live_publish_test.yml` (последний — моя добавка к исходному списку).
    Отдельно: удалить уже накопленные старые artifacts вручную — смена
    кода саму по себе не освобождает уже занятое место.

57. **Clarity Lab — minutes (требует отдельной проверки в своём
    репозитории, не здесь).** Убрать оба `Random delay` из runner (перенести
    рандomизацию времени публикации за пределы оплачиваемого раннера или
    фиксировать конкретное время). Сократить сбор комментариев Threads с
    трёх раз в день минимум до одного, не запускать, если функция не
    используется. Проверить, действительно ли нужны 3 Threads-поста в день.
    Проверить, почему более ранняя оптимизация (если коммит/PR реально
    существует) не оказалась в `main`.

**Про деньги:** текущие $4.46 полностью покрыты бесплatным лимитом, это не
долг. Реальный риск — не деньги, а то, что storage уже заполнен и новые
artifacts могут перестать загружаться.

**Открытый вопрос / зафиксированное намерение:** решения 56–57 нужно
перепроверить после того, как фикс будет закоммичен — отдельным заходом,
аналогично тому, как перепроверяются фиксы 54–55 в Части 11. Не считать
эту запись закрытой до факта проверки.

---

## 2026-07-25 — Часть 13: второй поток контента (evergreen/utility), граница территории ужесточена

**Контекст:** к 4 существующим публикациям в неделю (news-reactive,
Editorial Engine V2) предложено добавить 2 раза в неделю evergreen/utility
контент — не "что произошло в мире", а "что поможет бизнесу работать
лучше уже сегодня". Разбор шести предложенных категорий показал два
системных риска: часть примеров (автоматизация vs найм, "есть ли процесс
без вашего участия") утекала в общую территорию малого бизнеса, которую
решение 13 явно закрыло для Never Blank; часть форматов ("последние
десять компаний", "что мы наблюдаем среди малого бизнеса") предполагала
клиентскую базу наблюдений, которой у Never Blank сегодня физически нет
(0 клиентов) — прямое нарушение решения 10.

**Решения:**

58. **Второй поток контента подтверждён как новый Signal Type через тот
    же Editorial Engine и голос Never Blank**, не отдельная рубрика и не
    новая инфраструктура. Дёшево по разработке — использует уже
    существующее поле `content_role` (`education`/`proof`), не требует
    новой архитектуры. 2 раза в неделю, в дополнение к 4 существующим.

59. **Граница территории (решение 13) ужесточена явным списком
    исключений: как нанимать людей, как строить процессы, как управлять
    финансами, как масштабировать команду — всегда и без исключений
    территория VG Group, никогда не Never Blank**, независимо от формата
    (news-reactive или evergreen/utility). Это закрывает лазейку, которую
    открыли примеры вроде "автоматизировать процесс или нанять человека"
    и "есть ли у вас процесс, который работает без вас" — они звучат
    полезно, но это общий бизнес-совет, не про присутствие/видимость
    конкретно. Проверочный вопрос для нового сигнала: если заменить в
    примере "сайт"/"публикации" на "склад" или "бухгалтерию" и пример
    всё ещё логичен — сигнал не на территории Never Blank.

60. **Одобренная честная формулировка для контента о накопленных
    паттернах** (снимает конфликт с решением 10 для форматов вроде
    "Mistakes We Keep Seeing" и "What We're Seeing This Week"): опираться
    на реально существующий research-корпус (`config/research_sources.yaml`,
    `discover.py`), не на подразумеваемую клиентскую базу. Разрешено: "в
    исследуемых нами за последние две недели сигналах снова и снова
    встречается один и тот же паттерн". Запрещено: "мы видим...", "у
    последних десяти компаний, с которыми мы работали...", любая
    формулировка, подразумевающая клиентскую базу, которой сегодня нет.
    Когда у Never Blank появятся реальные клиентские наблюдения — можно
    будет пересмотреть, не раньше.

**Открытая идея (не решено сегодня, только зафиксировано намерение
исследовать):** контент по территории VG Group (найм, процессы, финансы,
масштабирование) не выбрасывается целиком — рассматривается вариант
сделать его отдельным форматом (интервью) на личном профиле Вали в
LinkedIn, а не на бренд-странице Never Blank. Отдельный канал, отдельная
аудитория, требует отдельной проработки — не часть текущей задачи.

---

## 2026-07-28 — Часть 14: архитектура utility-потока (Часть 13/58) — синтез двух независимых research-отчётов, квоты, метаданные

**Контекст:** deep research из решения 58 выполнен дважды независимо — один
раз мной (Claude, `strategy/research_utility_signal_archetypes_2026-07-25.md`,
12 архетипов), один раз параллельно GPT по тому же промпту
(`strategy/gpt_report_utility_archetypes_2026-07-28.pdf`, 20 архетипов).
Оба независимо сошлись в главном (Presence Check — топ-1 архетип,
AI Visibility — сильная и малозанятая тема, checklist/script форматы
выигрывают по saves) — это совпадение при независимой методологии само по
себе усиливает доверие к выводу. Расхождения разобраны и сведены в один
синтез, с двумя проверенными по коду поправками и двумя количественными
уточнениями.

Отдельно поймана методологическая проблема в отчёте GPT: сравнительная
таблица из 20 архетипов заявляла три независимых ранжирования (long-term
value / save likelihood / share likelihood), но при проверке колонки
long-term value и save likelihood оказались идентичны построчно для всех
20 строк — то есть фактически не два независимых расчёта, а один,
продублированный. Численное ранжирование из этого отчёта не считается
доказательством и не используется как таковое — только качественные
архетипы, источники, детализация форматов и YAML/QA-каркас.

**Решения:**

61. **Utility-контент организован в три зонтичные продуктовые категории**,
    а не в фиксированный список конкретных архетипов: `Presence Check`
    (самопроверки/аудиты/red flags), `Presence Script` (готовые
    формулировки и действия — запрос/ответ на отзыв, reactivation после
    паузы, переписывание bio/описания услуги), `Visibility Framework`
    (модели принятия решений, объяснения механизмов — AI visibility,
    discoverability, trust, consistency). Editorial Engine ротирует
    конкретные форматы внутри категорий, не привязан к одному
    повторяющемуся посту.

62. **Фирменные понятия (Presence Debt, Compound Presence, Trust Before
    Contact, Continuous Presence) — не четвёртая категория и не подтип
    `Visibility Framework`, а сквозной смысловой слой**, который может
    накладываться на любую из трёх категорий (пример: Presence Check про
    признаки накопленного Presence Debt; Presence Script про то, что
    опубликовать, чтобы начать его погашать). Без этого слоя utility-поток
    рискует стать неотличимым от контента любого другого аккаунта с
    советами для малого бизнеса — что прямо противоречит цели решения 2
    (узнаваемость, а не просто польза).

63. **Метаданные: используется существующее поле `content_role`
    (`ContentRole` enum, `src/strategy/models.py` — уже wired в
    `ContentPlanItem`), новое поле `content_family` НЕ вводится.**
    Проверено по коду: `ContentRole` уже включает значение `recognition`
    с определённым смыслом; параллельное поле `content_family` со своим
    отдельным значением `recognition` создало бы два разных понятия под
    одним именем — конфликт, которого решение 58 («используем
    `content_role`, не требует новой архитектуры») explicitly избегало.
    Вместо этого добавляются три новых, ничего не дублирующих поля:
    `utility_category` (`presence_check` / `presence_script` /
    `visibility_framework`), `strategic_topic` (`ai_visibility` /
    `discoverability` / `trust` / `consistency` / `recognition` /
    `positioning` / `reputation`), `brand_concept` (`presence_debt` /
    `compound_presence` / `trust_before_contact` / `continuous_presence` /
    `null`). Технически это требует явно добавить эти три поля в саму
    pydantic-модель `ContentPlanItem` — pydantic по умолчанию молча
    отбрасывает необъявленные поля, значит без явного добавления в модель
    QA-проверки по этим тегам будут читать пустоту. Это остаётся
    небольшим, но настоящим изменением кода, не просто разметкой поверх
    существующих данных.

64. **Квота AI Visibility: не менее 40% utility-публикаций в каждом
    тестовом цикле (8 недель) должны иметь `strategic_topic =
    ai_visibility`.** Выбрано 40%, а не 50% (при cadence 2/неделю 1 пост в
    неделю фиксированно на эту тему дал бы 50%) — потому что тестируется
    гипотеза "AI Visibility — одно из сильнейших направлений", а не
    "Never Blank = AI Visibility"; более высокая доля не даёт остальным
    strategic_topic развиться до появления данных.

65. **Квота фирменных понятий: не менее 60% utility-публикаций на первом
    этапе должны естественно использовать хотя бы один `brand_concept`
    (`!= null`).** Не 100% — иначе термины будут вставляться искусственно
    в каждый пост; не 0%/без квоты — иначе Editorial Engine может забыть о
    фирменных понятиях на недели. Долю разрешено пересматривать вверх,
    когда станет видно, какие понятия реально "цепляются" за аудиторию.

66. **Численное ранжирование архетипов из отчёта GPT (`gpt_report_
    utility_archetypes_2026-07-28.pdf`) не используется как источник
    истины** — long-term value и save likelihood колонки в нём
    оказались построчно идентичны для всех 20 архетипов при независимой
    проверке, то есть не два расчёта, а один. Из отчёта берутся только
    качественные архетипы, источники, детализация Presence Script/
    Visibility Audit форматов и YAML/QA_RULES-каркас как черновик для
    Claude Code.

**QA-правила (черновик для CI/Claude Code, из отчёта GPT, адаптирован):**
territory_check (совет остаётся валиден при замене "присутствия" на
"склад"/"бухгалтерию" → FAIL), source_check (внешние утверждения только
из публичных/официальных источников), observation_check ("в публичных
сигналах/в профилях/в поиске повторяется..." — не "мы видим у клиентов"),
saveability_check (пост содержит переиспользуемый инструмент — чек-лист,
скрипт, матрица, а не только мнение), self_contained_check (работает без
перехода по ссылке), voice_check (спокойный, точный тон — не hype/sales),
novelty_check (utility-угол отличается от последних 8 utility-постов по
поверхности/проблеме/типу инструмента).

---

## 2026-07-28 — Часть 15: Visibility Intelligence stream реализован (commit `6846f88`) — найден баг в квоте brand_concept

**Контекст:** Claude Code реализовал и запушил в `main` второй поток
публикаций (переименован из "utility" в "Visibility Intelligence" —
продуктовое название, код остался на `visibility_*`/`vi_*`, не
`utility_*`, шире, чем изначально предлагалось, но принято как факт).
Реализация включает: `ProductCategory`/`StrategicTopic`/`BrandConcept`
enum'ы и три новых поля на `ContentPlanItem` (`src/strategy/models.py`),
CSV-сериализацию (риск из impact analysis закрыт), `validate_visibility_
quota()` (`src/strategy/validators.py`), отдельный пайплайн генерации
`src/editorial/vi_pipeline.py` (partial regeneration по полям, не через
`generate_article()`), очередь `data/strategy/visibility_queue.jsonl`,
отдельный workflow `.github/workflows/visibility_publish.yml` (Tue/Thu),
35 тестов, все зелёные.

Проверено по коду (не только со слов отчёта): `ProductCategory` = `presence_
check`/`presence_script`/`visibility_framework` — соответствует решению
61. `BrandConcept` = `compound_presence`/`presence_debt`/`trust_before_
contact`, с комментарием `# verified NB term` на каждом значении —
соответствует списку Вали из обсуждения №4 (без `presence_system`/
`ai_findability`/`trust_layer`, которые предлагал первый бриф).

**Найденная проблема:** `StrategicTopic` так и остался с 3 значениями
(`ai_visibility`/`brand_concept`/`customer_trust`) вместо согласованных в
решении 63 семи, и коллизия имени `brand_concept`-как-тема vs
`brand_concept`-как-поле (пойманная до начала реализации, но не
исправленная до мёржа) дала конкретный измеримый баг:
`src/strategy/validators.py:440` считает квоту 60% (решение 65) как
`sum(1 for i in items if i.strategic_topic == StrategicTopic.BRAND_CONCEPT)`
— то есть считает посты, у которых **тема** помечена "brand_concept", а
не посты, у которых реально проставлено фирменное понятие
(`i.brand_concept is not None`, как требовало решение 65). Квота AI
Visibility (решение 64, та же функция, строка 439) посчитана правильно —
`strategic_topic == AI_VISIBILITY` это ровно то, что требовалось.

Тесты (`tests/strategy/test_visibility_quota.py`, все 17 boundary-кейсов)
построены вокруг `strategic_topic` и ни разу не варьируют поле
`brand_concept` независимо — то есть "35/35 зелёных" подтверждает
самосогласованность кода с самим собой, а не соответствие решению 65.

**Статус: найдено, НЕ исправлено.** Ничего не отправлено Claude Code по
этому пункту — запись только для протокола. Перед dry-run/первым Tue/Thu
cron нужно: (1) убрать `StrategicTopic.BRAND_CONCEPT` как значение,
заменить на что-то из семёрки решения 63 (например `positioning`),
(2) переписать подсчёт на `sum(1 for i in items if i.brand_concept is not
None)`, (3) досчитать `StrategicTopic` до полного списка решения 63.

---

## 2026-08-01 — Часть 16: тройной баг публикации (Instagram-картинка, design-version кеш, дублирование постов) — найден и исправлен, commit `b4acc0c`

**Контекст:** на recognition-потоке (не Visibility Intelligence) обнаружены
три связанные проблемы на production-пути. Диагностика Claude Code, затем
каждый пункт fix'а проверен мной напрямую в коде, а не принят со слов
отчёта.

**Что было не так:**

- Instagram получал тот же URL картинки (1920×1080, blog-размер), что и
  все остальные платформы — `platform_images` содержал корректные
  1080×1350 Instagram-ассеты, но они нигде не читались.
- `CURRENT_DESIGN_VERSION = "3"` существовал в коде, но нигде не
  сохранялся в пакете и не проверялся — `generate_and_publish.py` смотрел
  только "есть ли URL картинки", не "актуальна ли версия дизайна", так что
  правки дизайна не доходили до продакшена, пока кеш не протухал вручную.
- Сигнал `da56d75c98d25d55` (Lucid) опубликован 4 раза в Wix/LinkedIn/
  Facebook, 3 раза в Instagram за период 27 июля – 1 августа: workflow
  падал на `git push` с 403 на каждом запуске (не было права
  `contents: write`), поэтому `published_signal_ids.txt` никогда не
  попадал в репозиторий и следующий запуск снова выбирал тот же сигнал
  как неопубликованный.

**Фикс (проверено построчно в коде, не только по описанию коммита):**

- `DraftPackage.image_for(platform)` — `src/publishing/base.py:42-44`,
  возвращает `platform_image_urls[platform]` с фолбэком на `image_url`.
  Подтверждено, что все четыре паблишера реально его используют:
  `linkedin.py:56`, `instagram.py:44`, `facebook.py:43`, `wix.py:147`.
- Design-version инвалидация — `scripts/generate_and_publish.py:234-241`:
  регенерация запускается, если `_design_version` в кеше отсутствует или
  не совпадает с `CURRENT_DESIGN_VERSION`. `prepare_content.py` теперь
  проставляет `_design_version` в `platform_images`.
- `permissions: contents: write` добавлен в
  `.github/workflows/research_generate_and_publish.yml:43-44`, плюс
  `git pull --rebase --autostash` (строка 165) — снимает 403 и гонку с
  незакоммиченными файлами, которая его вызывала.

Commit `b4acc0c`, реально в `main` (проверено `git log`/`git show` —
сообщение коммита и диф совпадают с тем, что описано выше).

**Статус: исправлено и подтверждено в коде.** Первый чистый прогон —
ближайший понедельник, 07:00 UTC (recognition-поток). Отдельно от
незакрытого пункта Часть 15 (баг квоты `brand_concept` в
Visibility Intelligence) — это два разных потока, разные баги, только этот
закрыт.

---

## 2026-08-01 — Часть 17: разбор UPS-поста, root cause "корпорация как герой", разделение code/config/strategy/prompts, план Stage 1/Stage 2

**Контекст:** review реально опубликованного recognition-поста про
инвестицию UPS в cold-chain логистику вскрыл сразу и разовый дефект текста,
и системную проблему уровнем выше — где физически живёт бизнес-логика
Never Blank в репозитории. Обе части разобраны и проверены по коду, не
только со слов.

**По качеству конкретного поста (текстовые правила, применимые ко всем
будущим recognition-постам):**

67. **Новостной сигнал обязан быть явно связан с текстом абзацем-мостиком**,
    а не оставаться только в изображении/хештегах. Если картинка несёт факт
    ("$48M в cold-chain"), а текст его не упоминает — источник выглядит
    "приклеенным", а не использованным как доказательство. Нужен явный
    переход: что конкретно назвала компания, почему называние важно, как
    это иллюстрирует механизм Never Blank.
68. **Заголовок на картинке и тезис текста должны совпадать по смыслу**, не
    просто оба относиться к одной новости. Factual headline ("$48M
    investment...") и текст про invisible specialization — два разных
    сообщения в одном посте.
69. **Never Blank должен быть явно, жирным шрифтом назван в тексте поста**,
    не только подразумеваться. Пост без явного упоминания бренда мог бы
    опубликовать любой positioning-консультант — это подрывает накопление
    brand capital, а не просто стилистическая недоработка. Дополняет
    решение 52 (тэглайн после Echo разрешён) — сейчас проблема шире: сам
    бренд может отсутствовать в теле текста вообще.
70. **Хештеги: 3 брендовых + 3 отраслевых (решение 53) — подтверждено
    повторно.** В разобранном посте это правило снова нарушилось (все 5
    хештегов отраслевые) — не новое решение, а сигнал, что решение 53 не
    закреплено enforcement-проверкой и продолжает соскальзывать.
71. **Источник указывается по имени издания, не голым сокращённым URL**
    ("Source: UPS / [publisher name]", не "Source: lnkd.in/..."). Короткая
    ссылка сама по себе не создаёт доверия у читателя.

**Root cause (проверено в коде, не предположение):** в
`scripts/research/discover.py:121`, внутри захардкоженного system prompt
для отбора сигналов, есть строка: *"Large company M&A, earnings, product
launches — unless they directly show a MECHANISM relevant to small
business visibility"*. Это explicit исключение, разрешающее крупным
компаниям становиться источником сигнала, если LLM сочтёт, что там есть
подходящий механизм. Источники (`config/research_sources.yaml` — Small
Business Trends, SCORE, Entrepreneur, Inc., HubSpot, Search Engine
Journal, HBR, Axios Business, CNBC Small Business, все проверены) в целом
корректны и не про большой бизнес напрямую — утечка происходит на этапе
отбора, не на этапе списка источников. UPS в тексте — не сбой скоринга, а
предсказуемое следствие этой строки промпта: компания стала героем поста
именно потому, что правило это явно допускает при "механизме".

**Архитектурная проблема, вскрытая тем же местом кода:** философия Never
Blank, определение "что такое сигнал", примеры good/bad, аудитория и
правила отклонения — всё это захардкожено прямо в Python
(`discover.py:98–130`), а не вынесено в конфигурацию/стратегию/промпты.
Это противоречит изначальному замыслу модульности (чтобы нетехнический
клиент мог менять аудиторию/территорию через YAML, не трогая код).

72. **Принят целевой принцип разделения слоёв:** код определяет
    *способность* (fetch, dedupe, вызов LLM, скоринг, паблиш — универсально
    для любого клиента); конфигурация (`config/research_sources.yaml` и
    аналоги) определяет *источники*; стратегия (`strategy/current/`)
    определяет *смысл* — аудиторию, территорию, философию, правила
    отклонения; промпты (`config/prompts/`) определяют *редакционный
    голос*. Сейчас граница между слоями нарушена именно в
    `discover.py` — часть стратегии и философии живёт в Python-коде.

73. **Рефакторинг разбит на два явно разделённых этапа, порядок
    фиксированный.** Stage 1 — вынести захардкоженные промпт/бизнес-правила
    из `discover.py` в `config/prompts/research/` и `strategy/`, **без
    намеренного изменения поведения пайплайна** (тот же сигнал до/после
    рефакторинга должен давать функционально эквивалентный результат),
    закончить регрессионным прогоном на существующем сигнале. Stage 2 —
    только после зелёного Stage 1: менять саму редакционную логику и
    расширять определение "сигнала". Причина порядка — не абстрактная
    аккуратность: три инцидента этой сессии (Stellantis, вчерашний
    brand_concept-баг) произошли именно потому, что архитектурные и
    содержательные изменения смешивались в одном заходе и регрессию
    нельзя было локализовать.

**Открытая, не решённая сегодня идея (Stage 2, только направление, не
задание к исполнению):** расширить определение "сигнала" для
recognition-потока за пределы корпоративных новостей — включить публичные
наблюдения практиков (бухгалтеры, юристы, консультанты, MSP) и сообществ
(Reddit, LinkedIn-обсуждения, вопросы владельцев бизнеса). Направление
согласуется с решением 60 (честные, проверяемые публичные сигналы), но
требует отдельного решения по двум пунктам, которые сегодня не закрыты: (а)
один пост/видео практика — это анекдот, не "повторяющийся паттерн", и
формулировка в тексте должна честно отражать источник ("один консультант
написал...", не "мы видим повторяющийся паттерн"); (б) Reddit/YouTube/
LinkedIn технически не RSS — механика получения данных (adapters) и вопросы
ToS/атрибуции не проработаны, нужен отдельный технический ответ до начала
реализации. Отдельно предложена (тоже не решено) новая структура 4
recognition-дней: Пн — Research signal, Ср — Expert signal, Пт — Community
signal, Вс — Pattern signal. Явно подтверждено: это касается только 4
recognition-дней (Mon/Wed/Fri/Sun) и не пересекается с потоком Visibility
Intelligence (Tue/Thu, решения 58–66) — два независимых потока остаются
независимыми.

**Смена ролей в разработке (зафиксировано для протокола, не решение по
продукту) — версия 1, продержалась меньше одной сессии:** код пишет Codex;
Claude Code переходит в роль code review вместо рефакторинга.

**Смена ролей — версия 2 (актуальная), 2026-08-01, тот же день:** первая
попытка сразу столкнулась со структурной проблемой — Codex работает в
изолированном git-клоне, артефакты (например `stage1a_research_prompt_
refactor.patch`) не попадают в общий репозиторий автоматически, нужен
ручной шаг "выгрузить файл в чат". Это уже второй такой случай за проект,
не разовая случайность — полагаться на "не забыть выгрузить" признано
ненадёжным. Роли пересмотрены: **Val — архитектура, ТЗ, code review; Claude
Code — реализация в основном репозитории (прямой доступ на чтение/запись,
как и раньше); Codex — только локальные эксперименты, spike-прототипы,
анализ**, не критический путь до продакшена. Запись решений — особенно всех
решений по тексту/контенту — остаётся за этим протоколом
(`decision_log.md`) вне зависимости от того, какой инструмент пишет код в
моменте, чтобы решения не дрейфовали при очередной смене инструмента.

---

## 2026-08-06 — Часть 18: SpaceX-сигнал (ложная предпосылка) — Stage 1A и Enrichment Readiness fix подтверждены смерженными, Stage 1.5 не начат

**Контекст:** сигнал `SIGNAL_ID=4c39b34194e08b43` ("The average SpaceX
buyer post-IPO is almost under water after two-day slide") прошёл Discovery
и Scoring (`ARTICLE_READINESS_SCORE: 10`), но был корректно остановлен на
Enrichment: `CORE_FACT` — премиса не верифицируется, SpaceX не проводил
IPO; `REAL_COMPANY_EXAMPLE`/`SOURCE_FOR_CASE` пустые, `RECOMMENDED_FOR_
ARTICLE: false`. **Важно: ничего не опубликовано неправильно** — это не
повтор Stellantis/croissant, это подтверждение, что существующий
предохранитель (`_NULL_CASE` в `enrich.py`) сработал как задумано.
Диагностика (Claude Code, "no code changes made") и разбор GPT независимо
подтвердили: (а) вся research-цепочка (Discovery/Scoring/Enrichment) не
знает текущую стратегию — проверено: `strategy_context`/`active_strategy`
не встречается ни в `discover.py`, ни в `score.py`, ни в `enrich.py`,
только в `publish_packages.py`; (б) `RECOMMENDED_FOR_ARTICLE` имел два
разных смысла на разных стадиях — `score.py` ставил его по числовому
порогу, `enrich.py` мог позже перезаписать в `false` — подтверждено в коде.

**Что реально сделано и проверено (не со слов, напрямую в репозитории):**

74. **Stage 1A завершён и смёржен** — commit `b444c06`. Промпты
    research-этапов вынесены из Python в `config/prompts/research/`
    (`signal_selector.yaml`, `signal_enrichment.yaml`, `enrich.yaml`,
    `score.yaml`, `angles.yaml`, `content_package.yaml`). Захардкоженная
    строка `"You are a signal analyst..."` в `discover.py` подтверждённо
    отсутствует.

75. **Enrichment Readiness fix завершён и смёржен** — три коммита
    (`c4c9dd6`, `e663fe0`, `03c6ca6`), шире изначального "smallest safe
    fix", но по делу: `score.py` теперь пишет отдельное
    `SCORE_RECOMMENDED_FOR_ARTICLE` (только по порогу очков);
    `enrich.py` получил `determine_article_readiness()` — детерминированную
    (без LLM) функцию, которая ставит `SOURCE_PREMISE_VERIFIED` и
    `ARTICLE_READY` по двум путям доказательства (верифицированный кейс
    компании, ИЛИ верифицированные research/data при `CONFIDENCE >=
    medium`) и явно проверяет `CORE_FACT` на слова-маркеры недостоверности
    ("not verified", "unverified", "unverifiable", "unsupported", "cannot
    be confirmed"); `run_daily_research.py` теперь требует оба флага;
    `generate_and_publish.py` получил fail-closed preflight — сигнал без
    `ARTICLE_READY=true` блокируется по умолчанию, обход только через явный
    `FORCE_PUBLISH_OVERRIDE`/`APPROVED_OVERRIDE` с логируемым WARNING и
    записью в `force_overrides`. Всё проверено напрямую в
    `scripts/research/enrich.py` — код соответствует описанию коммитов.
    575 тестов проходят, заявлено 0 новых регрессий (сами тесты не
    перезапускались из этой сессии).

76. **Stage 1.5 (`ResearchContext`/`EditorialContext`) — НЕ начат.** В коде
    есть только TODO-комментарий в `c4c9dd6`. ТЗ не написано, файлы под
    изменение не определены, список инвариантов не сформулирован. Это
    следующий шаг, отдельный от уже сделанного, с тем же режимом "без
    изменения поведения + regression test", что и Stage 1A.

**Процессный урок:** первая независимая проверка Codex не смогла
подтвердить/опровергнуть мёрж именно из-за смены ролей версии 1 (Часть 17)
— Codex проверял устаревший локальный checkout (ветка `work`, коммит
`9688d32`) без доступа к `main` и без трёх реальных коммитов. Проверка
была методологически честной (не выдала предположение за факт), но
практически бесполезной. Для любой будущей независимой проверки — Codex
или кто угодно вне основного репозитория — обязательное условие: свежий
`main`, иначе результат формально корректен, а по сути проверяет не то
состояние репозитория.

---

## 2026-08-06 — Часть 19: оба конфликта закрыты — decision_log закоммичен, ветка business-investigation-layer заархивирована без полной проверки истории

**Контекст:** после Части 18 оставались два открытых конфликта: (1)
`decision_log.md` существовал только как несохранённые локальные правки
(Части 15–18), не виден никому вне этой сессии; (2) вывод Codex "ветку
`feature/business-investigation-layer` нельзя мержить, опасно" не
сошёлся с прямой git-проверкой — `merge-base(main, branch)` совпал с
самой веткой, то есть вершина ветки формально уже предок `main`, а
настоящий `git merge` физически не может удалить файлы из `main`. Один
факт (файл `docs/INVESTIGATION_QUESTION_LIBRARY.md` есть на ветке,
отсутствует в `main` при формальном "предке") остался необъяснённым —
вероятно, ветку когда-то уже вливали, а позже часть содержимого удалили
из `main`, но это не проверено до конца.

**Решения:**

77. **Конфликт 1 закрыт и подтверждён.** `.git/index.lock` удалён,
    `strategy/decision_log.md` вместе с `strategy/research_utility_
    signal_archetypes_2026-07-25.md` и `strategy/gpt_report_utility_
    archetypes_2026-07-28.pdf` закоммичены и запушены одним коммитом,
    `main` на `52dc144`. Проверено напрямую: `git log`, `git status`
    (чисто, кроме постороннего `reports/card_audit/`), оба файла физически
    в репозитории, `decision_log.md` показывает Части 1–18 без разрывов.
    `decision_log.md` снова является источником истины не только на
    словах.

78. **Конфликт 2 закрыт решением, не полной проверкой — осознанно.**
    Выбран вариант "принять практический план Codex как есть": ветку
    `feature/business-investigation-layer` не мержить и архивировать
    (переименовать/пометить `[archived]`, не удалять), из неё забрать
    только `docs/LERA_OPERATING_SYSTEM.md` и `docs/INVESTIGATION_QUESTION_
    LIBRARY.md` как docs-only референс для контракта Stage 1.5, никакого
    python/yaml/workflow-кода с ветки не трогать. **Явно зафиксировано:
    обоснование "почему нельзя мержить" не проверено до конца и, вероятно,
    ошибочно** (см. Часть 18) — решение принято ради практического
    результата (не тратить время на полную git-археологию ради ветки,
    из которой всё равно нужны только два файла), а не потому что
    формальная проверка подтвердила риск.

79. **Разблокировано: можно приступать к ТЗ Stage 1.5.** База чистая
    (решение 77), вопрос по ветке закрыт (решение 78). Следующий шаг —
    техническое задание на `ResearchContext`/`EditorialContext`, на базе
    `main @ 52dc144` (включает `03c6ca6`), с учётом двух cherry-picked
    документов как референса, без активации Curiosity Engine в рамках
    самого Stage 1.5 (решение из Части 18, не изменилось).

---

## 2026-08-06 — Часть 20: Stage 1.5 ТЗ v1 — request changes, не approve

**Контекст:** Claude Code прислал первую версию ТЗ Stage 1.5
(`ResearchContext`/`EditorialContext` как typed dataclass-контракты вместо
raw dict между стадиями research-пайплайна). GPT дала архитектурный
review с вердиктом "направление верное, ТЗ — request changes". Прежде чем
согласиться, самое серьёзное утверждение GPT проверено напрямую в коде,
не принято на веру.

**Решения:**

80. **ТЗ Stage 1.5 v1 не одобрено. Реализация не начинается.** Направление
    (typed lifecycle вместо словаря) — approve. Конкретный контракт — нет,
    по трём блокирующим причинам.

81. **Подтверждено прямым чтением кода: буквальная реализация сломала бы
    Editorial Engine.** `src/editorial/decision_lens_lite.py` и
    `src/editorial/narrative_spine.py` читают сигнал как словарь —
    `signal.get("HEADLINE", "")`, `signal.get("CORE_TENSION", "")`,
    `signal.get("BUSINESS_LESSON", "")`, `signal.get("STRATEGY_PRIMARY_
    MESSAGE")` и далее, десятки таких вызовов, ключи в верхнем регистре.
    У обычного Python `dataclass` метода `.get()` нет — передача
    `EditorialContext` вместо dict уронит эти модули на первом вызове.
    Утверждение исходного ТЗ "`src/editorial/*` — без змін" было
    фактически неверным без отдельного boundary-адаптера. Это не
    архитектурная вкусовщина — проверяемый, воспроизводимый факт.

82. **Ещё два блокирующих пробела в контракте (логический анализ, не
    требовал чтения кода, но признан обоснованным):** (а) состояние
    `score_only` одновременно включено в `ArticleStatus` и объявлено
    в самом ТЗ "теоретично неможливим" — недостижимое состояние в типе
    без объяснения, на какой стадии жизненного цикла оно вообще
    существует; неясно, живёт ли `ResearchContext` весь research-путь
    или только после `enrich.py` — ТЗ пытается делать оба одновременно;
    (б) `ResearchContext.to_editorial()` физически нереализуем как
    описано — `EditorialContext` требует `core_tension`, `business_
    lesson`, `never_blank_angle`, `possible_signature_line`, `potential_
    hook`, `target_audience` и три angle-поля, которых нет среди полей
    `ResearchContext`; откуда они берутся (typed-поле, strategy config,
    отдельный adapter, аргумент фабрики) — не определено. Поле `_raw`
    не принимается как источник этих значений — это обход типизации,
    а не её часть.

83. **Требования к ТЗ v2, зафиксированы как обязательные, не
    рекомендательные:** (1) однозначная state-transition таблица с учётом
    `stage`; (2) чёткая продолжительность жизни `ResearchContext` — весь
    pipeline или только post-enrichment, не оба сразу; (3) field-by-field
    маппинг, откуда каждое поле `EditorialContext` берётся; (4)
    спецификация legacy-совместимого boundary-адаптера
    (`EditorialContext → legacy dict с uppercase-ключами → существующий
    Editorial Engine`), переписывание самих editorial-модулей — вне
    scope Stage 1.5; (5) каноническая сериализация без утечки `_raw`
    в JSONL, `_raw` не читается downstream в обход типов; (6) runtime-
    инварианты (`Literal` ничего не валидирует сам по себе;
    `article_status`/`article_ready`/`force_override` не должны
    расходиться — либо вычисляются фабрикой, либо валидируются в
    `__post_init__`); (7) матрица behavior-equivalence тестов (legacy
    dict → context → dict обратно; старый и новый selection gate
    совпадают на одной матрице входов; preflight и editorial payload
    до/после идентичны).

84. **Investigation Evidence Gate не путать с сегодняшней factual
    readiness.** Поле-placeholder под будущий Curiosity Engine (Часть 18)
    разрешено оставить в контракте, но явно как неактивный статус, не
    как уже работающий gate — не должно использоваться как publishing
    authorization до Stage 2.

**Статус:** ТЗ отправлено обратно Claude Code с этим списком. Реализация
Stage 1.5 не начата.

---

## 2026-08-07 — Часть 21: Stage 1.5 закрыт — реализация влита в `main` коммитом `c51176b`

**Контекст:** Stage 1.5 (`ResearchContext`/`EditorialContext`) прошёл
первоначальную реализацию и две итерации исправлений, включая два раунда
review, перед финальным APPROVE.

**Хронология реализации:**

85. **Коммит `531be13` — первоначальная реализация Stage 1.5.** Созданы
    `src/lifecycle/signal_lifecycle.py` (`ResearchContext`, `EditorialContext`,
    `derive_factual_readiness()`, `derive_admission_status()`,
    `_KNOWN_JSONL_KEYS`) и `tests/test_signal_lifecycle.py` (43 теста). Selection gate
    в `run_daily_research.py` и preflight в `generate_and_publish.py`
    переведены на `ResearchContext`. `generate_article()` получает
    `rc.to_legacy_dict()` вместо raw dict.

86. **Первый review — REQUEST CHANGES по двум блокирующим пунктам.**
    (а) `EditorialContext` определён, но не подключён в production pipeline:
    `generate_article()` по-прежнему получал `rc.to_legacy_dict()`, минуя
    `EditorialContext` полностью. (б) Новый preflight использовал
    `rc.admission_status`, который требовал `SCORE_RECOMMENDED=true` —
    а оригинальный preflight проверял только `ARTICLE_READY` или override.
    Это недокументированное изменение поведения: сигналы с `ARTICLE_READY=true`
    и `SCORE_RECOMMENDED=false` раньше проходили publish, теперь
    блокировались.

87. **Коммит `4ad2433` — исправление preflight и фиксация scope
    `EditorialContext`.** Preflight возвращён к прежней семантике:
    `not rc.article_ready and not rc.force_override` — score больше не
    является publish-gate. `EditorialContext` явно задокументирован в
    docstring модуля как контракт Stage 1.5 с production wiring,
    сознательно перенесённым в Stage 2: `to_editorial()` требует
    `admission_status != 'rejected'`, но publish gate пропускает сигналы
    с `article_ready=True, score_recommended=False`, у которых
    `admission_status='rejected'` — подключение без изменения семантики
    gate требует отдельного решения. Добавлены parametrized-тесты:
    `test_selection_gate_parametrized`, `test_publish_preflight_parametrized`,
    `test_publish_preflight_score_not_a_gate`,
    `test_editorial_context_wiring_deferred_to_stage2` (+24 теста, итого 67).

88. **Второй review — REQUEST CHANGES по трём пунктам.** (а) В
    `test_preflight_equivalence` в блоке диагностики mismatch использовалась
    несуществующая переменная `new_ar_str` — при реальном расхождении тест
    падал бы с `NameError` вместо отчёта. (б) Parametrized-тесты заявляли
    проверку `APPROVED_OVERRIDE`, но фактически передавали только
    `FORCE_PUBLISH_OVERRIDE` — alias-путь не покрыт. (в) Фраза "те же 16
    pre-existing failures" не была доказана сравнением baseline и
    implementation — нужен был явный запуск на `52dc144` через `git worktree`.

89. **Коммит `904fc16` — исправление всех трёх дефектов.** `new_ar_str`
    заменён на `rc.article_ready`. Оба parametrized-теста получили отдельный
    параметр `ao_override` с кейсами: `APPROVED_OVERRIDE=true`
    без `FORCE_PUBLISH_OVERRIDE`; оба отсутствуют; конфликтующие значения
    (`AO=true+FP=false`, `FP=true+AO=false`, оба `false`). Baseline
    `52dc144` прогнан через `git worktree`: те же 16 тестов, те же
    причины, те же source-строки — ни один из файлов-нарушителей
    (`src/editorial/platform_composer.py`, `src/content/generator.py`,
    `src/publishing/wix_media.py`) не затронут Stage 1.5. Итого: 77
    тестов в suite Stage 1.5, полный suite 642 passed / 16 pre-existing
    failed.

90. **Финальный вердикт: APPROVE на `904fc16`.** Независимый review
    подтвердил исправление всех трёх дефектов, корректность semantics
    preflight, честность scope `EditorialContext`, ограниченность diff
    четырьмя файлами Stage 1.5.

91. **PR №2 merged в `main` коммитом `c51176b`.** Stage 1.5 закрыт.
    Ветка `feature/stage-1.5-typed-lifecycle` исчерпана.

**Открытый пункт для Stage 2:**

Production wiring `ResearchContext → EditorialContext → Editorial Engine`.
`to_editorial()` требует `admission_status` ∈ `{"admitted", "force_override"}`,
но publish gate пропускает сигналы с `article_ready=True, score_recommended=
False` (`admission_status="rejected"`). Подключение `EditorialContext`
в production path требует явного решения: либо расширить `to_editorial()`
на случай `article_ready=True` без `score_recommended`, либо ужесточить
publish gate (с документированием как intentional behavior change).

**Статус:** Stage 1.5 закрыт. Изменения влиты в `main` merge-коммитом
`c51176b`. Текущий HEAD `main` на момент фиксации: `74db296`.
